<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:task="http://www.springframework.org/schema/task"
       xsi:schemaLocation="http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
                           http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
                           http://www.springframework.org/schema/task https://www.springframework.org/schema/task/spring-task.xsd">

    <mvc:annotation-driven/>
    <mvc:resources mapping="/resources/**" location="/resources/"/>
    <mvc:resources mapping="/manifest.json" location="/"/>

    <context:component-scan base-package="ledger"/>

    <!-- 플레이스홀더 설정은 컨텍스트마다 따로 필요하다. 루트(context-datasource.xml)의 것은 이 컨텍스트의 @Value 에 적용되지 않는다 -->
    <context:property-placeholder location="classpath:/egovProps/globals.properties"/>

    <!-- @Scheduled 활성화: 고정 항목 자동 생성(매일 00:05). 스케줄 메서드를 가진 빈은 이 컨텍스트(ledger 패키지)에 둔다 -->
    <task:annotation-driven scheduler="scheduler"/>
    <task:scheduler id="scheduler" pool-size="1"/>

    <!-- 0순위: 빈 이름으로 뷰 찾기 (jsonView) -->
    <bean class="org.springframework.web.servlet.view.BeanNameViewResolver">
        <property name="order" value="0"/>
    </bean>
    <bean id="jsonView" class="org.springframework.web.servlet.view.json.MappingJackson2JsonView">
        <property name="contentType" value="application/json;charset=UTF-8"/>
    </bean>

    <!-- 1순위: JSP. 컨트롤러는 "dashboard/dashboardMain" 처럼 /WEB-INF/jsp/ledger/ 이하 경로를 반환한다.
         레이아웃(메뉴+본문)은 Tiles 대신 /WEB-INF/tags/layout.tag 로 각 JSP가 자기 본문을 감싼다. -->
    <bean id="viewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/jsp/ledger/"/>
        <property name="suffix" value=".jsp"/>
        <property name="order" value="1"/>
    </bean>

    <bean id="messageSource" class="org.springframework.context.support.ReloadableResourceBundleMessageSource">
        <property name="basenames">
            <list><value>classpath:/message/message-common</value></list>
        </property>
        <property name="defaultEncoding" value="UTF-8"/>
        <property name="cacheSeconds" value="60"/>
    </bean>

    <!-- 임시: 디자인 미리보기용 화면 매핑. 해당 화면의 @Controller 를 만들면 그 줄을 지운다.
         겹쳐도 오류는 나지 않고 @Controller 가 우선하지만, 남겨 두면 어느 쪽이 응답하는지 헷갈린다 -->
    <mvc:view-controller path="/ledger/entry" view-name="entry/entryMain"/>
    <mvc:view-controller path="/ledger/summary" view-name="summary/summaryMain"/>
    <mvc:view-controller path="/ledger/recurring" view-name="recurring/recurringMain"/>
    <mvc:view-controller path="/ledger/asset" view-name="asset/assetMain"/>
    <mvc:view-controller path="/ledger/settings" view-name="settings/settingsMain"/>

    <!-- 로그인 확인 인터셉터: /ledger/** 는 세션에 userId 가 있어야 접근 가능 -->
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/ledger/**"/>
            <bean class="ledger.interceptor.LoginPageInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>
</beans>
