# /etc/nginx/conf.d/ledger-limit.conf 로 복사한다 (conf.d 는 nginx.conf 의 http 블록에서 읽힌다).
# IP 당 분당 10회, deploy/nginx-ledger.conf 의 /p3/auth/(login|signup) 에서 쓴다.

limit_req_zone $binary_remote_addr zone=ledger_auth:1m rate=10r/m;
