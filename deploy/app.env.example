# 서버: sudo install -m 600 -o root -g root deploy/app.env.example /etc/ledger/app.env 후 값 입력
# 가입 코드가 비어 있으면 가입할 수 없다. 공개 저장소에 적힌 값은 쓰지 않는다.
LEDGER_DB_URL=jdbc:postgresql://127.0.0.1:5432/ledger
LEDGER_DB_USER=ledger_app
LEDGER_DB_PASSWORD=
LEDGER_SIGNUP_CODE=
LEDGER_HOLIDAY_API_KEY=
